/*
    需求：
        1、渲染 Emoji 列表
        2、扩展 Emoji
        3、emoji 兼容性判断
        4、emojiToHTML、emojiToSymbol、symbolToHTML、symboToEmoji
 */

var SuperEmoji = (function() {

    var Util = {
       
    };

    var emojis = {
        
    };

    var set = function(cfg) {

    };

    var get = function(){
      
    };

    var emojiToHTML = function(emoji) {
        
    };

    var emojiToSymbol = function(emoji) {

    };

    var symbolToHTML = function(symbol) {

    };

    var symboToEmoji = function(symbol) {

    };

    return { 
        set: set,
        get: get,
        emojiToHTML: emojiToHTML,
        emojiToSymbol: emojiToSymbol,
        symbolToHTML: symbolToHTML,
        symboToEmoji: symboToEmoji
    };
})();